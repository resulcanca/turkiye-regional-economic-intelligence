# Contributing

1. Create a branch.
2. Keep official source provenance for every added dataset.
3. Never commit synthetic values as real-world observations.
4. Add validation checks for new data.
5. Keep SQL compatible with SQLite unless a file explicitly targets PostgreSQL.
